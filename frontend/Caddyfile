{
}

:80 {
    # Backend service
    handle_path /api* {
        reverse_proxy localhost:8000
    }

    # Frontend SPA as static file
    handle {
        root * ./dist
        try_files {path} /index.html
        file_server
    }
}