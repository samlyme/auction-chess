{
    auto_https off
}

:{$PORT} {
    encode 

    # Backend service
    handle_path /api* {
        reverse_proxy {$BACKEND_URL}
    }

    # Frontend SPA as static file
    handle {
        root * ./dist
        try_files {path} /index.html
        file_server
    }
}