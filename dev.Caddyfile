{
  auto_https off
  log {
    level INFO
    format console
  }
}

:3000 {
  handle_path /api* {
    reverse_proxy localhost:8000
  }

  # Everything else → Vite
  handle {
    reverse_proxy localhost:5173
  }
}