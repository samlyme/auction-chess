{
  auto_https off
}

:80 {
  # Strip /api and proxy EVERYTHING (http + ws) to port 8000
  handle_path /api* {
    reverse_proxy localhost:8000
  }

  # Everything else → Vite
  handle {
    reverse_proxy localhost:3000
  }
}