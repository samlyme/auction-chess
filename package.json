{
  "name": "auction-chess",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "format": "prettier --write ./**",
    "build:edge": "bun run scripts/build-edge-function.ts"
  },
  "workspaces": [
    "shared",
    "server",
    "clients/web"
  ]
}
