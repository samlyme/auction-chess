{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "dev": "bun --hot --env-file=.env.development index.ts",
    "build": "bun build index.ts --target bun --outdir ./out --sourcemap",
    "start:local": "bun --env-file=.env.development out/index.js",
    "start:prod": "bun --env-file=.env.production out/index.js",
    "start": "bun out/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "hono": "^4.10.7"
  }
}
